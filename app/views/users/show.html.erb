<h1>users/show</h1>
<div class="row">
  <aside class="col-md-4">
    <section class="user.info">
      <h3>
        <%= #for_each @user.dog do |d| %>
        <%= link_to image_tag(dog_pic_for(@user), alt: "Pluto the dog") # TODO: replace with the user's dog image, or helper method to use stock/user's dog image%>
        <%= @user.dog %>
        <%= #end %>
      </h3>
    </section>
  </aside>
</div>
